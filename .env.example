# Database — SQLite for local dev, Postgres connection string for prod
DATABASE_URL=sqlite+aiosqlite:///./trace.db

# API key for authenticated endpoints
TRACE_API_KEY=tr_dev_change_me

# Logging level — DEBUG for dev, INFO for prod
LOG_LEVEL=DEBUG
